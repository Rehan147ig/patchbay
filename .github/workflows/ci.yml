name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: patchbay
          POSTGRES_PASSWORD: patchbay_dev_only
          POSTGRES_DB: patchbay
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U patchbay -d patchbay"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
      redis:
        image: redis:7-alpine
        ports: ["6379:6379"]
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.34.5
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm format:check
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
      - name: Apply migrations
        run: pnpm db:migrate
        env:
          DATABASE_URL: "postgresql://patchbay:patchbay_dev_only@localhost:5432/patchbay?schema=public"
      - name: Seed database
        run: pnpm db:seed
        env:
          DATABASE_URL: "postgresql://patchbay:patchbay_dev_only@localhost:5432/patchbay?schema=public"
      - run: pnpm build
        env:
          DATABASE_URL: "postgresql://patchbay:patchbay_dev_only@localhost:5432/patchbay?schema=public"
