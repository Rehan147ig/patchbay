[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "openai-python-legacy"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "openai>=1.30.0",
  "stripe>=8.0.0",
  "twilio>=9.0.0",
  "requests>=2.31.0",
]