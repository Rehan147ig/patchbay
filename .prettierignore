# Dependency caches
node_modules

# Build output
.next
out
dist
coverage

# Next.js regenerates this on every dev/build with its own formatting
# (and its header says not to edit it); gating it only produces churn.
apps/web/next-env.d.ts

# Other-session scratch files (transient PowerShell helpers at repo root)
*.ps1

# Local diagnostics
cookies.txt
payload.json
*-out.txt
q.sql
q2.sql

# pnpm owns the lockfile format; prettier only churns it
pnpm-lock.yaml
